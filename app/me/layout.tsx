import React from "react";

export default function MeLayout({ children }: { children: React.ReactNode }) {
  // Placeholder: personal tenant context loading coming soon
  return <div style={{ padding: 16 }}>{children}</div>;
}
